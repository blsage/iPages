import XCTest
@testable import iPages

@available(iOS 13.0, *)
final class iPagesTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(iPages().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
